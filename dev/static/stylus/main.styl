// Подключаем стили библиотек
@import '_libs.styl'

// Подключаем переменные
@import '_vars.styl'

// Подключаем спрайты (просто раскоментируем импорт при появлении PNG спрайтов)
@import '_sprite.styl'

// Подключаем миксины
@import '_mixins.styl'

html 
  line-height 1
  font-size 14px

body
  font-family mainFont
  color mainColor

h1,h2,h3,h4,h5,h6
  margin 0

ul,li
  margin 0
  padding 0
  list-style-type none
  
p
  margin 0
  
a
  text-decoration none
  color black

audio,
canvas,
iframe,
img,
svg,
video
  vertical-align middle
  
textarea
  resize none

section
  position relative
  padding 90px 0

input,
select,
button
  outline none

*, *:before, *:after
  box-sizing border-box
  
.hide
  display none
  
.show
  display block
  
::-moz-selection 
  background: #b3d4fc;
  text-shadow: none;
  
::selection 
  background: #b3d4fc;
  text-shadow: none;
  
.clearfix:before,
.clearfix:after 
  content: " ";
  display: table;

.clearfix:after 
  clear: both;

.clearfix 
  *zoom: 1;

.wrapper
  overflow hidden
  
.container
  position relative
  width 97%
  max-width 1170px
  margin 0 auto



// Общие стили
@import '_common_styles.styl'

// Стили шапки
@import 'modules/_header.styl'

// Стили меню
@import 'modules/_menu.styl'

// Стили главной страницы
@import 'page/_index.styl'

// Стили секции главый баннер
@import 'section/_top_banner.styl'

// Стили секции Who and Why
@import 'section/_who_and_why.styl'

// Стили секции Advantages
@import 'section/_advantages.styl'

// Стили секции Featured
@import 'section/_featured.styl'

// Стили секции Clients
@import 'section/_clients.styl'

// Стили секции Portfolio
@import 'section/_portfolio.styl'

// Стили секции Quote
@import 'section/_quote.styl'

// Стили секции Team
@import 'section/_team.styl'

// Стили секции Fact and Numbers
@import 'section/_fact-and-numbers.styl'

// Стили секции Articles
@import 'section/_articles.styl'

// Стили секции Articles Slider
@import 'section/_articles-slider.styl'

// Стили секции Twitter
@import 'section/_twitter.styl'

// Стили футера
@import 'modules/_footer.styl'

// Стили медиазапросов
@import '_media.styl'